{
  "name": "epic-spinners-wrapper",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {},
  "private": true,
  "scripts": {
    "serve:spinners": "yarn workspace epic-spinners dev",
    "build:spinners": "yarn workspace epic-spinners build",
    "serve:docs": "yarn workspace docs dev",
    "build:docs": "yarn workspace docs build",
    "preview:docs": "yarn workspace docs preview",
    "build:docs:ci": "yarn workspace docs build:ci",
    "build:ci": "yarn build:spinners && yarn build:docs",
    "serve:ci": "yarn serve packages/docs/dist"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
